#include "main.h"

/**
 * _strpbrk - searches a string for any set of chars
 * @s: the string
 * @accept: the set of chars
 *
 * Return: a pointer to the first occurence of accept
 */

char *_strpbrk(char *s, char *accept);
